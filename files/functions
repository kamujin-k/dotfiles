porta() {
    filter="sport = :$1"

    sudo ss -lptn $filter
}


reload() {
    if [[ $SHELL == *"zsh"* ]]; then
        source ~/.zshrc
    else
	source ~/.bashrc
    fi
}


b64(){
   echo -n $1 | base64 $2
}
